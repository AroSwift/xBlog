Class Login
end

	before(:each) dp
		@login = Login.new
		@login.extend(Say)
	end

	describe User::login do
		
		it "takes user username and password"
			@login.show.should == ""
			experct(login).to eq 
		end
	end